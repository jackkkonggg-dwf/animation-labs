import { TextFillPattern } from './text-fill-pattern';

export const metadata = {
  title: 'Text Fill Animation | GSAP Animation Patterns',
  description: 'Learn text fill animations with GSAP. Use background-clip: text technique to create color-fill-on-scroll effects that reveal text with beautiful gradients.',
};

export default function TextFillPage() {
  return <TextFillPattern />;
}
