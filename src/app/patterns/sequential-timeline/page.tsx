import { SequentialTimelinePattern } from './sequential-timeline-pattern';

export const metadata = {
  title: 'Sequential Timeline Pattern | GSAP Animation Showcase',
  description: 'Learn how to choreograph multiple animations with GSAP timelines',
};

export default function SequentialTimelinePage() {
  return <SequentialTimelinePattern />;
}
