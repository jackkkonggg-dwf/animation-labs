import { ScrubTimelinePattern } from '@/components/patterns/scrub-timeline-pattern';

export const metadata = {
  title: 'Scrub Timeline Pattern | GSAP Animation Showcase',
  description: 'Learn how to control animation playback with scroll position using GSAP timelines',
};

export default function ScrubTimelinePage() {
  return <ScrubTimelinePattern />;
}
