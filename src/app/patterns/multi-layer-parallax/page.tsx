import { MultiLayerParallaxPattern } from './multi-layer-parallax-pattern';

export const metadata = {
  title: 'Multi-Layer Parallax Pattern | GSAP Animation Showcase',
  description: 'Learn how to create multi-layer parallax effects with GSAP ScrollTrigger. Multiple layers moving at different speeds create rich, immersive 3D depth.',
};

export default function MultiLayerParallaxPage() {
  return <MultiLayerParallaxPattern />;
}
