import { WordTextRevealPattern } from './word-text-reveal-pattern';

export const metadata = {
  title: 'Word-by-Word Text Reveal | GSAP Animation Patterns',
  description: 'Learn word-by-word text reveal animations with GSAP. Split text into word spans and animate each with staggered delays for dramatic headlines.',
};

export default function WordTextRevealPage() {
  return <WordTextRevealPattern />;
}
