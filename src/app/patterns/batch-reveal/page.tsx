import { BatchRevealPattern } from './batch-reveal-pattern';

export const metadata = {
  title: 'Batch Reveal Pattern | GSAP Animation Showcase',
  description: 'Efficient batch animation technique for large grids of elements with GSAP ScrollTrigger.',
};

export default function BatchRevealPage() {
  return <BatchRevealPattern />;
}
